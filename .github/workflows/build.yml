name: Build
run-name: Build ${{ github.ref_name }}

on:
  workflow_dispatch:
    inputs:
      build_android:
        description: 'Build Android'
        required: true
        type: boolean
        default: true
      build_windows:
        description: 'Build Windows'
        required: true
        type: boolean
        default: true
      build_macos:
        description: 'Build macOS'
        required: true
        type: boolean
        default: true
      build_linux_x64:
        description: 'Build Linux x64'
        required: true
        type: boolean
        default: true
      build_linux_arm64:
        description: 'Build Linux ARM64'
        required: true
        type: boolean
        default: true

permissions:
  contents: read

jobs:
  Build_Android:
    if: ${{ inputs.build_android }}
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v5
      - run: rm -rf /opt/hostedtoolcache
      - uses: subosito/flutter-action@v2
        with:
          channel: "stable"
          flutter-version-file: pubspec.yaml
          architecture: x64
      - name: Decode and install certificate
        env:
          STORE_FILE: ${{ secrets.ANDROID_KEYSTORE }}
          PROPERTY_FILE: ${{ secrets.ANDROID_KEY_PROPERTIES }}
        run: |
          echo "$STORE_FILE" | base64 --decode > android/keystore.jks
          echo "$PROPERTY_FILE" > android/key.properties
      - uses: actions/setup-java@v5
        with:
          distribution: 'oracle'
          java-version: '17'
      - name: Configure Gradle plugin repositories
        run: |
          mkdir -p ~/.gradle/init.d
          cat > ~/.gradle/init.d/plugin-repositories.gradle <<'EOF'
          settingsEvaluated { settings ->
              settings.pluginManagement.repositories {
                  google()
                  mavenCentral()
                  gradlePluginPortal()
              }
          }
          EOF
      - name: Check rust-toolchain.toml
        run: rustup show
      - run: flutter pub get --enforce-lockfile
      - run: flutter build apk --release
      - name: Upload APK (armeabi-v7a)
        uses: actions/upload-artifact@v5
        with:
          name: apk-armeabi-v7a
          path: build/app/outputs/apk/release/*armeabi-v7a*.apk
      - name: Upload APK (arm64-v8a)
        uses: actions/upload-artifact@v5
        with:
          name: apk-arm64-v8a
          path: build/app/outputs/apk/release/*arm64-v8a*.apk
      - name: Upload APK (x86_64)
        uses: actions/upload-artifact@v5
        with:
          name: apk-x86_64
          path: build/app/outputs/apk/release/*x86_64*.apk
      - name: Upload APK (universal)
        uses: actions/upload-artifact@v5
        with:
          name: apk-universal
          path: build/app/outputs/apk/release/*-android.apk

  Build_Windows:
    if: ${{ inputs.build_windows }}
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v5
      - name: Install dependencies
        run: |
          choco install yq -y
          pip install httpx
      - name: Install Inno Setup
        run: choco install innosetup --no-progress
      - uses: subosito/flutter-action@v2
        with:
          channel: "stable"
          flutter-version-file: pubspec.yaml
          architecture: x64
      - name: Build Windows installer
        run: |
          flutter pub get --enforce-lockfile
          python windows/build.py
      - name: Validate Windows artifacts
        shell: pwsh
        run: |
          $zip = @(Get-ChildItem -Path build/windows -File -Filter 'VeneraNext-*-windows.zip')
          $installer = @(Get-ChildItem -Path build/windows -File -Filter 'VeneraNext-*-windows-installer.exe')
          if ($zip.Count -ne 1) { throw "Expected one Windows zip, found $($zip.Count)." }
          if ($installer.Count -ne 1) { throw "Expected one Windows installer, found $($installer.Count)." }
          $artifacts = @($zip + $installer)
          $empty = @($artifacts | Where-Object { $_.Length -le 0 })
          if ($empty.Count -gt 0) {
            throw "Windows artifacts contain empty files: $($empty.Name -join ', ')"
          }
          Add-Type -AssemblyName System.IO.Compression.FileSystem
          $expectedRoot = [System.IO.Path]::GetFileNameWithoutExtension($zip[0].Name)
          $archive = [System.IO.Compression.ZipFile]::OpenRead($zip[0].FullName)
          try {
            $badEntries = @($archive.Entries | Where-Object {
              -not $_.FullName.StartsWith("$expectedRoot/") -and $_.FullName -ne "$expectedRoot/"
            })
            if ($badEntries.Count -gt 0) {
              throw "Windows zip contains files outside $expectedRoot/: $($badEntries.FullName -join ', ')"
            }
            $exeEntry = @($archive.Entries | Where-Object { $_.FullName -eq "$expectedRoot/VeneraNext.exe" })
            if ($exeEntry.Count -ne 1) {
              throw "Windows zip does not contain $expectedRoot/VeneraNext.exe"
            }
          } finally {
            $archive.Dispose()
          }
          $artifacts | Sort-Object Name | ForEach-Object { "$($_.Name) $($_.Length) bytes" }
      - uses: actions/upload-artifact@v5
        with:
          name: windows_build
          path: |
            build/windows/VeneraNext-*-windows.zip
            build/windows/VeneraNext-*-windows-installer.exe
          if-no-files-found: error

  Build_MacOS:
    if: ${{ inputs.build_macos }}
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v5
      - uses: subosito/flutter-action@v2
        with:
          channel: "stable"
          flutter-version-file: pubspec.yaml
          architecture: x64
      - run: sudo xcode-select --switch /Applications/Xcode_16.4.app
      - run: flutter pub get --enforce-lockfile
      - name: Check rust-toolchain.toml
        run: rustup show
      - name: Build Flutter macOS App
        run: flutter build macos --release
      - name: Create DMG
        run: |
          mkdir -p dist
          mkdir -p dist/dmg_contents
          cp -R build/macos/Build/Products/Release/VeneraNext.app dist/dmg_contents/
          ln -s /Applications dist/dmg_contents/Applications
          hdiutil create -volname "VeneraNext" -srcfolder dist/dmg_contents -ov -format UDZO "dist/VeneraNext.dmg"
      - name: Add version to filename
        run: |
          APP_VERSION=$(sed -n 's/^version:[[:space:]]*//p' pubspec.yaml | tr -d '\r\n')
          mkdir -p result
          mv dist/VeneraNext.dmg "result/VeneraNext-$APP_VERSION.dmg"
      - name: Upload DMG
        uses: actions/upload-artifact@v5
        with:
          name: macos_build
          path: result/

  Build_Linux_x64:
    if: ${{ inputs.build_linux_x64 }}
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v5
      - uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          flutter-version-file: pubspec.yaml
          architecture: x64
      - run: |
          flutter pub get --enforce-lockfile
          sudo apt-get update -y
          sudo apt-get install -y ninja-build libgtk-3-dev webkit2gtk-4.1
          dart pub global activate -s git https://github.com/venera-app/flutter_to_debian.git
      - run: python3 debian/build.py x64
      - run: python3 .github/scripts/build_arch_package.py
      - run: |
          sudo rm -rf build/linux/arch/app.tar.gz
          sudo rm -rf build/linux/arch/pkg
          sudo rm -rf build/linux/arch/src
          sudo rm -rf build/linux/arch/PKGBUILD
          sudo rm -rf build/linux/arch/Dockerfile
      - uses: actions/upload-artifact@v5
        with:
          name: deb_build
          path: build/linux/x64/release/debian
      - uses: actions/upload-artifact@v5
        with:
          name: arch_build
          path: build/linux/arch/

  Build_Linux_ARM64:
    if: ${{ inputs.build_linux_arm64 }}
    runs-on: ubuntu-22.04-arm
    steps:
      - uses: actions/checkout@v5
      - uses: subosito/flutter-action@v2
        with:
          channel: 'master'
          flutter-version-file: pubspec.yaml
      - run: |
          flutter pub get --enforce-lockfile
          sudo apt-get update -y
          sudo apt-get install -y ninja-build libgtk-3-dev webkit2gtk-4.1
          dart pub global activate -s git https://github.com/venera-app/flutter_to_debian.git
      - name: Patch font
        run: dart run patch/font.dart
      - run: python3 debian/build.py arm64
      - uses: actions/upload-artifact@v5
        with:
          name: deb_arm64_build
          path: build/linux/x64/release/debian
